<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @restaurant.name %>
</p>

<p>
  <strong>Address:</strong>
  <%= @restaurant.address %>
</p>

<p>
  <strong>Number of seats:</strong>
  <%= @restaurant.number_of_seats %>
</p>

<p>
  <strong>Category:</strong>
  <%= @restaurant.category %>
</p>

<p>
  <%= image_tag(@restaurant.image_url.to_s) %>
</p>

<p>
  <div id="map"></div>
    <script>
      function initMap() {
        var geocoder= new google.maps.Geocoder();
        var address = {lat:<%= @restaurant.latitude %>, lng:<%= @restaurant.longitude%>};
        var map = new google.maps.Map(document.getElementById('map'), {
          zoom: 15,
          center: new google.maps.LatLng(<%= @restaurant.latitude %>, <%= @restaurant.longitude%>)
        });
        }
    </script>
    <script async defer
      src="https://maps.googleapis.com/maps/api/js?key=<%="AIzaSyCIgY-Sip4jTSSNzg6X43-VxyIh0QUwmiY"%>&callback=initMap">
    </script>
</p>

Lon:<%= @restaurant.latitude %>, lat:<%= @restaurant.longitude %>



<%= link_to 'Edit', edit_restaurant_path(@restaurant) %> |
<%= link_to 'Back', restaurants_path %>
